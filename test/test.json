{
    "test1": "这是一个test，值为：${test}(\\${test})",
    "test2": [
        "${test}",
        "${test}1",
        "${test}3"
    ],
    "name": "这是一个test，值为：${name}(\\${test})",
    "${name}": [
        "${name}",
        "${name}2",
        "${name}3"
    ],
    "${name}1": {
        "${name}": [
            "${name}2",
            "${name}3"
        ]
    }
}